namespace apu;

enum FRAME_ID : byte {
     TASK10MS = 1,
     TASK20MS = 1, 
     TASK40MS = 2
}

table apu_msg {
    frame_id        :  FRAME_ID = TASK10MS ;
    valid_flag      : long;
    receive_counter : long;
    send_counter    : long;
}

file_identifier "endi";

root_type apu_msg;