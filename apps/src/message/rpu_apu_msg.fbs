namespace rpu;

enum FRAME_ID : byte {
     TASK10MS = 1,
     TASK20MS = 1, 
     TASK40MS = 2
}

table rpu_msg {
    frame_id        :  FRAME_ID = TASK10MS ;
    valid_flag      : uint ;
    receive_counter : uint ;
    send_counter    : uint ;
}

file_identifier "endi";

root_type rpu_msg;