namespace endiag;

enum FRAME_ID : byte {
     TASK10MS = 1,
     TASK20MS = 1, 
     TASK40MS = 2
}

table FooBar {
    frame_id        :  FRAME_ID = TASK10MS ;
    valid_flag      : long;
    receive_counter : long;
    send_counter    : long;
}

file_identifier "ENDI";

root_type FooBar;